{
    "version": 2,
    "routes": [
      { "src": "/api/(.*)", "dest": "/api/$1" }
    ],
    "builds": [
      { "src": "api/**/*.js", "use": "@vercel/node" }
    ],
    "env": {
      "SUPABASE_URL": "@supabase-url",
      "SUPABASE_KEY": "@supabase-key"
    }
  }